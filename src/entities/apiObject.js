// apiConfig.js

// Define the API URL as a constant
const API_URL = 'https://localhost:4502/api';

// Create an API object that stores the API URL
const apiObject = {
  apiUrl: API_URL, // Assign the API URL to a property called 'apiUrl'
};

// Export the API object to make it accessible from other modules
export default apiObject;
